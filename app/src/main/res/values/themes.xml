<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- XML launch theme only; all in-app styling is done by the Compose design system in ui/theme. -->
    <style name="Theme.Discnct" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- For the shortcut target: it does its work and finishes, so it must never paint. -->
    <style name="Theme.Discnct.Invisible" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
